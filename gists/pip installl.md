windows：
1.升级python
```
python -m pip install -U pip
```

2.pip 不存在安装
```
python -m ensurepip
```

3.windows查看环境变量
```
(type env:path) -split ';'
```
4.windows查看环境变量不换行
```
$env:path
```